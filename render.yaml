services:
  - type: web
    name: dentvision-api
    runtime: node
    buildCommand: cd server && npm install && npx prisma generate && npx prisma db push --accept-data-loss
    startCommand: cd server && node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: ENCRYPTION_KEY
        generateValue: true
