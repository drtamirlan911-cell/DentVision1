package kz.dentvision.crm.navigation

import androidx.compose.runtime.Composable

/**
 * Экраны, которые действительно построены и работают на настоящих данных.
 *
 * Список пустой не потому, что что-то забыли: каркас — это первый этап, экраны
 * подключаются следующими, и каждый попадает сюда только вместе со своим
 * рабочим экраном. Пока идентификатора здесь нет, раздел не появляется ни в
 * меню, ни в графе маршрутов — открыть пустоту нельзя.
 */
val IMPLEMENTED_PAGES: Map<String, @Composable () -> Unit> = emptyMap()

/** Домашний маршрут оболочки. */
const val ROUTE_WORKSPACE = "workspace"
