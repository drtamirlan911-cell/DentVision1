# Android release-gate evidence

This file exists to keep the hosted Android build on the release-gate path. The workflow must pass `testDebugUnitTest` and `assemblePreview` and publish the preview APK artifact before Android execution is signed off.
